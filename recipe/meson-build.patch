--- meson.build 2026-02-19 13:03:23.623413707 +0200
+++ meson.build 2026-02-19 13:03:50.738686971 +0200
@@ -37,6 +37,56 @@
   'AdsLib/standalone/NotificationDispatcher.cpp',
 ])
 
+ads_root_headers = files(
+  'AdsLib/AdsDef.h',
+  'AdsLib/AdsDevice.h',
+  'AdsLib/AdsException.h',
+  'AdsLib/AdsFile.h',
+  'AdsLib/AdsLib.h',
+  'AdsLib/AdsNotification.h',
+  'AdsLib/AdsNotificationOOI.h',
+  'AdsLib/AdsVariable.h',
+  'AdsLib/AmsConnection.h',
+  'AdsLib/AmsHeader.h',
+  'AdsLib/AmsPort.h',
+  'AdsLib/AmsRouter.h',
+  'AdsLib/Frame.h',
+  'AdsLib/LicenseAccess.h',
+  'AdsLib/Log.h',
+  'AdsLib/NotificationDispatcher.h',
+  'AdsLib/RegistryAccess.h',
+  'AdsLib/RingBuffer.h',
+  'AdsLib/RouterAccess.h',
+  'AdsLib/Router.h',
+  'AdsLib/RTimeAccess.h',
+  'AdsLib/Semaphore.h',
+  'AdsLib/Sockets.h',
+  'AdsLib/SymbolAccess.h',
+  'AdsLib/wrap_endian.h',
+  'AdsLib/wrap_registry.h',
+  'AdsLib/wrap_socket.h',
+)
+install_headers(ads_root_headers, subdir: 'AdsLib')
+
+bhf_headers = files(
+  'AdsLib/bhf/ParameterList.h',
+  'AdsLib/bhf/StringToInteger.h',
+  'AdsLib/bhf/WindowsQuirks.h',
+)
+install_headers(bhf_headers, subdir: 'AdsLib/bhf')
+
+standalone_headers = files(
+  'AdsLib/standalone/AdsDef.h',
+  'AdsLib/standalone/AdsLib.h',
+)
+install_headers(standalone_headers, subdir: 'AdsLib/standalone')
+
+twincat_headers = files(
+  'AdsLib/TwinCAT/AdsDef.h',
+  'AdsLib/TwinCAT/AdsLib.h'
+)
+install_headers(twincat_headers, subdir: 'AdsLib/TwinCAT')
+
 inc = include_directories([
   'AdsLib',
   'tools',
@@ -50,6 +100,7 @@
 adslib = static_library('AdsLib',
   [common_files, router_files],
   include_directories: inc,
+  install: true,
 )
 
 adslib_dep = declare_dependency(
@@ -62,6 +113,8 @@
   include_directories: inc,
   dependencies: libs,
   link_with: adslib,
+  install: true,
+  install_dir: get_option('bindir'),
 )
 
 adslibooitest = executable('AdsLibOOITest',
@@ -69,6 +122,8 @@
   include_directories: inc,
   dependencies: libs,
   link_with: adslib,
+  install: true,
+  install_dir: get_option('bindir'),
 )
 
 adstool = executable('adstool',
@@ -76,6 +131,8 @@
   include_directories: inc,
   dependencies: libs,
   link_with: adslib,
+  install: true,
+  install_dir: get_option('bindir'),
 )
 
 if get_option('tcadsdll_include') != ''
@@ -86,6 +143,8 @@
       get_option('tcadsdll_include'),
       inc,
     ],
+    install: true,
+    install_dir: get_option('libdir'),
   )
 
   tcadsdll_deps = [
@@ -104,5 +163,7 @@
       tcadsdll_deps
     ],
     link_with: tcadslib,
+    install: true,
+    install_dir: get_option('bindir'),
   )
 endif
